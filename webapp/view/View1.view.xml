<mvc:View controllerName="helloui5.controller.View1"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:f="sap.ui.layout.form">
    <Page id="page" title="{i18n>salesTitle}" showHeader="true">
        
        <!-- <Text text="Hello from SAP UI5!" />
        <Button text="Click me" press=".onButtonPress" /> -->

        <!-- <headerContent>
            <Button text="Edit" press=".onEditPress"></Button>
        </headerContent>

        <f:SimpleForm xmlns:f="sap.ui.layout.form" editable="false" layout="ResponsiveGridLayout" title="Order Information">

        <f:content>

        <Label text="Order Number" />
        <Text text="{/orderNumber}" />

        <Label text="Customer" />
        <Text text="{/customer}" />

        <Label text="Status" />
        <Text text="{/status}" />

        <Label text="Net Value" />
        <Text text="{/netValue} {/currency}" />

        </f:content>

        </f:SimpleForm> -->

        <Table id="ordersTable" items="{/orders}">
            <headerToolbar>
                <Toolbar>
                    <Title text="Sales Order List"></Title>
                    <ToolbarSpacer />
                    <SearchField placeholder="search order..." width="30%" search=".onSearch"></SearchField>
                </Toolbar>
            </headerToolbar>

            <columns>
                <Column><Text text="Order No" /></Column>
                <Column><Text text="Customer"></Text></Column>
                <Column><Text text="Status"></Text></Column>
                <Column><Text text="Net Value"></Text></Column>
                <Column><Text text="Currency"></Text></Column>
            </columns>

            <items>
                <ColumnListItem >
                    <cells>
                        <Text text="{orderNumber}" />
                        <Text text="{customer}" />
                        <Text text="{status}" />
                        <Text text="{netValue}" />
                        <Text text="{currency}" />
                    </cells>
                </ColumnListItem>
            </items>

        </Table>

    </Page>
</mvc:View>